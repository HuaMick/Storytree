// @storytree/forest-world-r3f — the ADR-0123 THIRD forest-world mapper: R3F/three.
// The provability firewall (the r3f-world-spike capability): the pure world-to-3D
// descriptor mapping (`world-to-3d.ts` — no React/three imports, node:test-provable)
// is the machine-proven heart; the thin R3F canvas + drei MapControls harness beside
// it are browser glue whose look is witnessed, not machine-judged (ADR-0070).
//
// Scaffold barrel: the leaf authors `world-to-3d.ts` red→green; this barrel
// re-exports the layers as they land.
export {};
