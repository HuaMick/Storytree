// Compatibility seam for Studio callers while the app-surface adapter leaf switches TreeView.
export {
  arrivalGrowPlan,
  REVEAL_STAGGER_MS,
  trailRevealPlan,
  type RevealSegment,
  type TrailDir,
  type TrailRevealPlan,
} from '@storytree/app-surface';
